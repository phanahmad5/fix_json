{
  "name": "news-api-server",
  "version": "1.0.0",
  "description": "Fake REST API for news using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
